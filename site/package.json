{
  "name": "lexicity-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "index": "pagefind --site dist --output-subdir pagefind",
    "build:pages": "astro build && pagefind --site dist --output-subdir pagefind"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.0.0"
  }
}
